[project]
name = "learning-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.59.0",
    "pydantic-ai",
    "asyncpg",
    "python-dotenv",
    "loguru",
    "fastapi",
    "uvicorn",
]
